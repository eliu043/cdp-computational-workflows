// Copy this file to `mapbox-token.js`, then paste a public `pk...` Mapbox token.
// GitHub Pages must publish that file because browser maps cannot keep public
// client tokens secret. Never put a secret `sk...` token in client-side code.
window.MAPBOX_TOKEN = "pk.your-public-mapbox-token-here";
